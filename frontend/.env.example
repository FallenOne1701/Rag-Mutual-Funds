# Chat API base URL.
# Leave as /api to use the Vite dev proxy (frontend :5173 -> API :8000).
# For a static build served elsewhere, set the full origin, e.g. http://localhost:8000
VITE_API_BASE_URL=/api
