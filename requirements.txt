# Mutual Fund FAQ Assistant — Phase 0+ dependencies
# LLM: Groq only. Embeddings: local sentence-transformers.

# API
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0

# Groq LLM (locked sole provider)
groq>=0.11.0

# Config / HTTP / HTML
pyyaml>=6.0.2
httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.3.0
trafilatura>=1.12.0

# Embeddings + vector store (used from Phase 1)
sentence-transformers>=3.0.0
chromadb>=0.5.0

# Tests
pytest>=8.3.0
